/**
 * Global constants used across frontend
 */

// Default page size for product listing & pagination
export const PAGE_SIZE = 20;

// Maximum recently viewed products to store
export const MAX_VIEW_HISTORY = 10;

// Default currency (matches backend)
export const DEFAULT_CURRENCY = "GBP";

// App name (used in UI / meta if needed)
export const APP_NAME = "Product Data Explorer";
